FLASK_ENV=development
SECRET_KEY=replace-with-a-long-random-string
JWT_SECRET_KEY=replace-with-a-different-long-random-string
# Leave DATABASE_URL unset to use the default SQLite file (backend/kwality.db).
# For production, set e.g. DATABASE_URL=postgresql://user:pass@host:5432/kwality
# DATABASE_URL=
